name: delline
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_screenutil: ^5.1.0
  pin_code_fields: ^7.0.0-nullsafety
  flutter_bloc: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_native_splash: ^1.3.3

  flutter_lints: ^1.0.0
flutter_native_splash:
  color: '#E8B526'
  image: assets/app_logo/app_logo_black.png
  android: true
  ios: true

flutter:

  uses-material-design: true

  assets:
    - assets/app_logo/
    - assets/app_icon/
    - assets/brend_img/
    - assets/category_img/
